interface:
  display_name: "TikTokDownloader"
  short_description: "Repo-specific patterns and workflows for TikTokDownloader"
  default_prompt: "Use the TikTokDownloader repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true